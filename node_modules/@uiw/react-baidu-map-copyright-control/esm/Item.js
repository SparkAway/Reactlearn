import { useEffect, useState } from 'react';
import { usePrevious, useRenderDom } from '@uiw/react-baidu-map-utils';
var uid = 1;
export function CopyrightControlItem(props) {
  if (props === void 0) {
    props = {};
  }

  var {
    children,
    control,
    bounds
  } = props;
  var [id] = useState(uid++);
  var {
    container
  } = useRenderDom({
    children
  });
  var prevId = usePrevious(id);
  useEffect(() => {
    if (control) {
      control.addCopyright({
        id,
        bounds,
        content: container.innerHTML
      });
    }

    return function () {
      if (prevId && control) {
        control.removeCopyright(prevId);
      }
    };
  }, [children, control]);
  return null;
}
//# sourceMappingURL=Item.js.map